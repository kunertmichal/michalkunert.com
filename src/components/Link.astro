---
export interface Props {
	href: string;
}

const { href } = Astro.props;
---

<a href={href} class="syne inline-flex rounded-full text-sm font-bold py-2 px-8 bg-gradient-to-br from-[#9600C0] to-[#5654CA] focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 whitespace-nowrap">
    <slot />
</button>
